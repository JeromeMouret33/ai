# Backend — dépendances (Phase 1+). À épingler au fur et à mesure.
fastapi
uvicorn[standard]
httpx          # appels OpenRouter (API compatible OpenAI)
pyyaml         # lecture config (fragments + params)
python-dotenv  # chargement .env
pydantic

# Phase 2 — stockage & livraison
# Supabase : accès via REST (httpx, déjà listé) — pas de dépendance dédiée.
google-api-python-client   # Google Drive
google-auth                # Auth OAuth Drive

# Dev / tests
pytest
