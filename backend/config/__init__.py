"""Couche de configuration : fragments de prompt + paramètres.

Tout est éditable dans l'app (pas de prompt en dur dans le code).
- prompt_fragments.yaml : fragments modulaires assemblés selon angle + options.
- params.yaml           : 3 modèles, ratio, résolution, candidats, toggles, logo, Drive.
"""
