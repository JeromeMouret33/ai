# Fragments de prompt modulaires — éditables dans l'app.
# Doc humain canonique : ../../prompts-config-goodcar.md (§2). Garder les deux synchro.
# Philosophie v2 : description positive d'UNE photographie cohérente ; les interdits
# sont concentrés dans `constraints` (un seul bloc), jamais répétés ailleurs.
# Variables injectées : {ANGLE}, {FRAMING_PRESET}, {LOGO_POSITION_SIZE}, {RATIO}, {RESOLUTION}.

role: |
  You are creating one coherent, professional automotive studio photograph.

vehicle_lock: |
  The vehicle image shows the exact, real car being sold. Your photograph must show
  this same physical car, faithfully: same model and trim, same paint colour, same
  wheels and rims, same grille, badges and lettering, and the same headlights and
  tail lights — reproduce their outline and internal LED / light-graphic pattern
  exactly as in the source. You are photographing a real object, not reinterpreting
  a design. You may photograph it from the showroom's natural eye-level camera at
  the requested angle — the viewpoint may differ from the source photo. Where the
  source is unclear or low quality, reproduce it as-is.

ref_showroom: |
  SHOWROOM image: the exact environment of the photograph — same white matte walls
  and corner, soft warm lighting, light-grey floor with a subtle reflection. The
  environment looks identical on every generation.

ref_logo: |
  LOGO image: the GOODCAR brand sign, mounted FLAT on the upper-left wall in its
  real colours — same shapes, text, colours and aspect ratio, crisp and legible.
  It reads as a printed sign in the brand's own colours (not an engraving, never
  tinted to match the wall). Position: {LOGO_POSITION_SIZE}.

ref_vehicle: |
  VEHICLE image — the exact car to feature.

ref_plate: |
  PLATE image — the GOODCAR license plate to apply to the vehicle.

task_base: |
  Photograph the car inside the showroom as a real photo taken on location, shown
  at: {ANGLE}. Natural eye-level camera, realistic proportions relative to the
  room, wheels firmly on the floor, with a soft contact shadow under the tires and
  a subtle reflection of the car on the floor. The showroom's soft, even, slightly
  warm studio light falls naturally on the car.

relight: |
  Give the car the studio look: even out the source photo's sun glare, blown
  highlights and harsh shadows on the smooth body panels and side/rear glass,
  replacing them with the showroom's gentle, even highlights. This adjusts how
  light falls on the car — what the car is (its shapes, colours and components)
  stays exactly as in the vehicle image.

plate: |
  Replace the visible license plate(s) with the plate from the plate image, fitted
  in correct perspective, undistorted.

composition: |
  COMPOSITION ({ANGLE}): {FRAMING_PRESET}

constraints: |
  HARD RULES:
  - Same car throughout: model, paint colour, wheels, grille, badges, and the
    exact original headlight / tail light patterns.
  - Showroom identical to the showroom image; nothing moved, recoloured or added.
  - Logo exactly as provided: once, flat, real colours, undistorted.
  - No added text, watermarks, people or extra objects.
  - Output {RATIO}, {RESOLUTION}, photoreal, clean.

interior: |
  INTERIOR: this is a cabin photograph — reproduce the cabin exactly as in the
  vehicle image, with all interior details, materials and colours as-is. The only
  change: everything visible through the windows becomes clean, evenly blown-out
  white. No showroom, no logo, no relighting.

# Presets de cadrage par angle (doc §6). slug -> { angle: {ANGLE}, framing: {FRAMING_PRESET} }.
# La classification renvoie un slug ; l'assembleur injecte angle + framing dans `composition`.
angle_presets:
  face-avant:
    angle: "front"
    framing: "straight front, centered, slightly low camera."
  3-4-avant-gauche:
    angle: "front 3/4 left"
    framing: "angled, front-left visible, slightly low camera, car ~70% of frame width, centered."
  3-4-avant-droit:
    angle: "front 3/4 right"
    framing: "mirror of front 3/4 left: angled, front-right visible, slightly low camera, car ~70% of frame width, centered."
  profil-gauche:
    angle: "profile left"
    framing: "full side, eye-level, ~80% frame width."
  profil-droit:
    angle: "profile right"
    framing: "full side, eye-level, ~80% frame width."
  3-4-arriere-gauche:
    angle: "rear 3/4 left"
    framing: "rear visible, same logic (angled, slightly low camera, car ~70% of frame width, centered)."
  3-4-arriere-droit:
    angle: "rear 3/4 right"
    framing: "rear visible, same logic (angled, slightly low camera, car ~70% of frame width, centered)."
  arriere:
    angle: "rear"
    framing: "straight rear, centered, slightly low camera."
  interieur:
    angle: "interior"
    framing: ""   # variante `interior`, pas de showroom
  detail:
    angle: "detail"
    framing: "close-up; neutral background or per decision."

# --- Instructions des agents vision (HORS pack version A — ajout Phase 1, à affiner) ---
# Utilisées par pipeline/classify.py et pipeline/qc.py. Éditables comme les autres fragments.
# {ANGLES} est injecté avec la liste des slugs valides au moment de l'appel.

classify_instruction: |
  You are an automotive photo angle classifier. Look at the single vehicle photo
  and decide from which viewpoint the car (or its interior) is shown.
  Respond with STRICT JSON only, no prose:
    {"angle": "<one slug from the list>", "confidence": <float 0..1>}
  The slug MUST be exactly one of: {ANGLES}.
  Guidance:
    - "face-avant": straight front. "arriere": straight rear.
    - "3-4-avant-gauche"/"3-4-avant-droit": front three-quarter, left/right side visible.
    - "3-4-arriere-gauche"/"3-4-arriere-droit": rear three-quarter, left/right side visible.
    - "profil-gauche"/"profil-droit": full side profile, left/right.
    - "interieur": cabin / interior shot.
    - "detail": close-up of a part (wheel, badge, light…), not a full-car angle.
  "confidence" is your certainty in the chosen slug.

qc_instruction: |
  You are a strict quality controller for AI-generated automotive showroom photos.
  Image 1 is the AI-GENERATED showroom photo. Image 2 (if provided) is the ORIGINAL
  source photo of the vehicle — use it as the reference to judge fidelity.
  Verify on image 1:
    1. The showroom background is clean (white matte walls, soft warm light,
       light-grey floor) with no extra objects, text or people, and the car sits
       naturally in the room (eye-level perspective, realistic proportions, wheels
       on the floor).
    2. The GOODCAR logo (if present) is flat, in its real colours, crisp, legible,
       undistorted, placed once on the upper-left wall, not overlapping the car.
    3. The car MATCHES the source vehicle (image 2): same model, paint colour,
       wheels/rims, grille, badges. ZOOM IN on the headlights and tail lights:
       their outline and internal LED / light-graphic pattern must match the
       source — any invented, deformed, simplified or restyled optic is a failure.
       (A different camera angle or studio lighting than the source is FINE.)
  Respond with STRICT JSON only, no prose:
    {"verdict": "ok" | "ko", "reasons": ["<short reason>", ...]}
  Use "ok" only if every check passes. List concrete reasons for any "ko".
